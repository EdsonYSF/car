server:
  port: 9095
  servlet:
    context-path: /
spring:
  application:
    name: car-weChat
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        prefix: car-weChat
  profiles:
    active: dev
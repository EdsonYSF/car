server:
  port: 9091
  servlet:
    context-path: /
spring:
  application:
    name: car-admin
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: DEFAULT_GROUP
  profiles:
    active: dev